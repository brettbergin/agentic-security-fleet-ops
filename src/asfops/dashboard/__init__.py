"""asfops dashboard: a Streamlit view over the ~/.asfops/logs run corpus.

The parsing (:mod:`asfops.dashboard.data`) and launcher
(:mod:`asfops.dashboard.launch`) import no Streamlit; only ``app.py`` (run by
Streamlit itself) does. Install with ``pip install 'asfops[dashboard]'``.
"""
